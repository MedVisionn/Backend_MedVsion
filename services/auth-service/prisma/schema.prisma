generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../db.sqlite" // Make sure to use the centralized sqlite or individual ones
}

model AuthUser {
  id       String @id @default(uuid())
  email    String @unique
  password String
  role     String
}

